jalaliDatepicker.startWatch({
  persianDigits: false,
});

flatpickr("#flat", { locale: "fa" });
var picker = new Pikaday({ field: document.getElementById("pick") });

const elements = document.getElementById(".b");
